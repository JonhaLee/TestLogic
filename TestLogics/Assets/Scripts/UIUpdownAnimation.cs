using UnityEngine;
using System.Collections;

public class UIUpdownAnimation : MonoBehaviour {
    /*
    void Start()
    {
        InvokeRepeating("Down", 0, 1);
        InvokeRepeating("Up", 0.5f, 1);
    }

    void Down()
    {
        iTween.MoveBy(gameObject,
           iTween.Hash("y", -0.15f, "easeType", iTween.EaseType.easeOutQuart));
    }

    void Up()
    {
        iTween.MoveBy(gameObject,
           iTween.Hash("y", 0.15f, "easeType", iTween.EaseType.easeOutQuart));
    }
    */
   
}
